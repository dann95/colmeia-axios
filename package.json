{
  "name": "colmeia-axios",
  "private": true,
  "version": "1.0.0",
  "description": "Wrapper for Axios, used inside Colmeia applications.",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.21.1",
    "typescript": "^3.8.3"
  },
  "scripts": {
    "build": "tsc",
    "deploy": "tsc && cp index.d.ts dist/index.d.ts && cp package.json dist/package.json && cd dist && npm publish"
  }
}
